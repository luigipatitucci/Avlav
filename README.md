# Next.js + TypeScript + CSS Modules Starter

Plantilla mínima para un sitio web con Next.js, TypeScript y CSS Modules.

Requisitos: Node.js 18+ y npm.

En Windows (cmd.exe):

```
cd nextjs-ts-css
npm install
npm run dev
```

El proyecto usa el router tradicional de `pages`. Componentes usan CSS Modules en `components` y `styles`.
